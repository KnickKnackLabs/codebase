id: bats-public-task-path-shell-assignment
language: Bash
severity: hint
message: environment assignment before nested shell dispatch
rule:
  kind: variable_assignment
  inside:
    stopBy: end
    all:
      - kind: command
      - has:
          field: name
          regex: '^(bash|run)$'
