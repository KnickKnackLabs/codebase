id: variadic-args-consumer
language: Bash
severity: hint
message: command may consume a variadic Usage value
rule:
  all:
    - kind: command
    - has:
        kind: command_name
        regex: '^(eval|read)$'
        stopBy: neighbor
