id: bash-empty-argv-forwarding-candidate
language: Bash
severity: hint
message: braced positional expansion inside a quoted string
rule:
  all:
    - kind: expansion
    - regex: '^\$\{@\}$'
    - inside:
        kind: string
        stopBy: end
