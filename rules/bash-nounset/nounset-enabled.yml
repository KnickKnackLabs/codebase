id: bash-nounset-enabled
language: Bash
severity: hint
message: command enables nounset
rule:
  all:
    - kind: command
    - regex: '^set([[:space:]]+[^[:space:]]+)*[[:space:]]+(-[A-Za-z]*u[A-Za-z]*|-o[[:space:]]+nounset)([[:space:]]|$)'
