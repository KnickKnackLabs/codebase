id: bash-nounset-enabled
language: Bash
severity: hint
message: command enables nounset
rule:
  all:
    - kind: command
    - regex: '^set[[:space:]]+(-[A-Za-z]*u[A-Za-z]*([[:space:]]|$)|-o[[:space:]]+nounset([[:space:]]|$))'
